{% extends 'base.html.twig' %}

{% block body %}
    <h1>UsuarioCategoria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuarioCategorium.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ usuarioCategorium.estado }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usuariocategoria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('usuariocategoria_edit', { 'id': usuarioCategorium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
